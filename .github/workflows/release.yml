name: Release & Version Bump

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      version_bump:
        description: 'Version bump type (patch, minor, major)'
        required: false
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major

jobs:
  release:
    name: Release & Version Bump
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install release-tools
        run: npm install -g conventional-changelog-cli semver
      
      - name: Get current version
        id: current_version
        run: |
          CURRENT_VERSION=$(grep '^version = "' Cargo.toml | head -1 | cut -d'"' -f2)
          echo "current=$CURRENT_VERSION" >> $GITHUB_OUTPUT
          echo "Current version: $CURRENT_VERSION"
      
      - name: Determine new version
        id: new_version
        run: |
          CURRENT="${{ steps.current_version.outputs.current }}"
          BUMP_TYPE="${{ github.event.inputs.version_bump || 'patch' }}"
          
          case $BUMP_TYPE in
            patch)
              NEW_VERSION=$(echo $CURRENT | awk -F. '{print $1"."$2"."($3+1)}')
              ;;
            minor)
              NEW_VERSION=$(echo $CURRENT | awk -F. '{print $1"."($2+1)".0"}')
              ;;
            major)
              NEW_VERSION=$(echo $CURRENT | awk -F. '{print ($1+1)".0.0"}')
              ;;
          esac
          
          echo "new=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "New version: $NEW_VERSION"
      
      - name: Update Cargo.toml versions
        run: |
          NEW_VERSION="${{ steps.new_version.outputs.new }}"
          sed -i 's/^version = "[0-9]\+\.[0-9]\+\.[0-9]\+"/version = "'$NEW_VERSION'"/g' Cargo.toml
          
          # Update individual package versions
          for TOML_FILE in */Cargo.toml; do
            if grep -q "version.workspace = true" "$TOML_FILE"; then
              continue
            fi
            sed -i 's/^version = "[0-9]\+\.[0-9]\+\.[0-9]\+"/version = "'$NEW_VERSION'"/g' "$TOML_FILE"
          done
          
          echo "Updated all Cargo.toml files to version $NEW_VERSION"
      
      - name: Generate Changelog
        run: |
          NEW_VERSION="${{ steps.new_version.outputs.new }}"
          conventional-changelog -p angular -r 0 > CHANGELOG.md
          echo "## [${NEW_VERSION}] - $(date +%Y-%m-%d)" >> CHANGELOG.md
          echo "" >> CHANGELOG.md
          echo "### Changed" >> CHANGELOG.md
          echo "- Bump version to ${NEW_VERSION}" >> CHANGELOG.md
          echo "" >> CHANGELOG.md
          echo "### Added" >> CHANGELOG.md
          git log --pretty=format:"- %s" $(git describe --tags --abbrev=0 HEAD^)..HEAD >> CHANGELOG.md 2>/dev/null || true
      
      - name: Commit version bump
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Cargo.toml */Cargo.toml CHANGELOG.md
          git commit -m "chore: bump version to ${{ steps.new_version.outputs.new }}"
      
      - name: Push changes
        run: git push
      
      - name: Create Release Tag
        run: |
          NEW_VERSION="${{ steps.new_version.outputs.new }}"
          git tag "v${NEW_VERSION}"
          git push origin "v${NEW_VERSION}"
          echo "Created tag v${NEW_VERSION}"
